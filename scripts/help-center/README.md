# scripts/help-center

ראו `docs/help-center/README.md`.

לכידה: דורש שרת מקומי (`npm run dev`) וחשבון דמו `ADMIN` / `1234`. אופציונלי: `E2E_PARENT_*` לצילומי הורה.

```bash
npm run dev
npm run help:capture -- --base-url=http://127.0.0.1:3001
npm run help:publish-screenshots
```
