# בדיקות ידניות — מרכז עזרה

**תאריך:** 2026-05-23  
**סביבה:** `http://127.0.0.1:3001` (לאחר הפעלה מחדש של `npm run dev`)  
**מבצע:** סוכן (§14.1 — שלב לפני אישור סופי)

## מקלדת ומיקוד

- [x] קישור «דלג לתוכן» קיים (`a[href="#help-main"]`) — נבדק ב־`/help` באמצעות Playwright (headless)
- [x] מחלקות `focus-visible` ב־DOM — נוכחות ב־HTML של `/help`
- [ ] Tab / Shift+Tab על כל קישורי `/help/**` — לא נבדק במלואו בדפדפן אינטראקטיבי (דורש סשן ידני)

## כותרות ו־RTL

- [x] **47/47** נתיבי `/help/**` מחזירים HTTP 200, מכילים `dir="rtl"` ו־`<h1>`
- [x] `npm run build` — 89 עמודים סטטיים, כולל כל מדריכי העזרה (§ ניתוב)

## נגישות תמונות

- [ ] לכל תמונה בפרסום יש `alt` + `<picture>` — **חסום**: אין קבצים ב־`public/help-center/screenshots/` (צילום לא הושלם)

## ניגודיות (דגימה)

- [ ] בדיקת כלי נגישות בדפדפן — לא בוצעה (דורשת UI ידני)

## דפדפנים

- [ ] Chrome / Firefox / Safari — לא בוצע (סוכן: Chromium headless בלבד לדגימת `/help`)

## הערות

- אוטומציית axe-core אינה מאושרת בתוכנית.
- לאחר תיקון חשבון הדמו וצילום מסך — יש לחזור על סעיף «נגישות תמונות» ולוודא `<picture>` בדפדפן.
