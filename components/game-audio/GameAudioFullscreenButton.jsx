import GameAudioSettingsButton from "./GameAudioSettingsButton.jsx";

/** @deprecated Use GameAudioSettingsButton — kept for existing imports. */
export default function GameAudioFullscreenButton(props) {
  return <GameAudioSettingsButton {...props} />;
}
