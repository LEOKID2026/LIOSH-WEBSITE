/** Fixed demo data generator version — bump intentionally only; part of every seed. */
export const PARENT_DEMO_DATA_VERSION = 1;

/** Fixed history anchor — never rolls forward with calendar. */
export const DEMO_HISTORY_START = "2026-03-25";

export const DEMO_PARENT_SUBJECTS = Object.freeze([
  "math",
  "hebrew",
  "english",
  "geometry",
  "science",
]);

export const DEMO_PARENT_BEARER_TOKEN = "demo-parent-portal";

export const DEMO_DASHBOARD_RECENT_ACTIVITIES_LIMIT = 10;

export const DEFAULT_PARENT_DEMO_STUDENT_LIMIT = 3;
