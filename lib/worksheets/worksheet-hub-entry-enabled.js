/**
 * Temporary kill-switch for the parent-dashboard "דפי עבודה להדפסה" entry button (UI only).
 * Flip to `true` to re-enable navigation without other changes.
 */
export const WORKSHEET_HUB_ENTRY_ENABLED = false;
