/**
 * Mirror of offline-flags for public/student/sw.js (static SW cannot import app modules).
 * Keep in sync with lib/offline/offline-flags.js.
 */
/* eslint-disable no-unused-vars */
const STUDENT_OFFLINE_FULL_SW_ENABLED = true;
